import { FormInputLabelDirective } from './form-input-label.directive';

describe('FormInputLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputLabelDirective();
    expect(directive).toBeTruthy();
  });
});
